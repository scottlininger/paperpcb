# AGENTS.md

## Working Rules
- Never make code changes unless the proposed changes are shown inline in chat first and the user approves them.
- Prefer showing small inline diffs for proposed edits.
- When running commands the user asked for, show as much compile/upload/output progress inline in chat as possible.
- Treat this file as the standing workflow for work in this folder unless the user overrides it.
